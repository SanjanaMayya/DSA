# Write a program to swap the case of each character in a string.
text = input("Enter a string: ")
# Swap the case of each character in the string
swapped_text = text.swapcase()
print("String with swapped case:", swapped_text)